---
title: Getting Started
navigation: true
description: Hackbox is a real-time multiplayer game framework that allows you to create games where players use their mobile devices as controllers. Just bring your own websocket-compatible game, and let Hackbox handle all the networking!
---

## Overview

A Hackbox game consists of three main components:

1. **Player Client** - Mobile interface that runs on player devices
2. **Your Host App** - Your own app or game that controls the player experience
3. **Game Server** - Backend that interfaces the player clients with your host app

## Connecting to Hackbox

### 1. Create a room

First, your app needs to create a new room. These rooms are ephemeral and will remain open for only 24 hours.

To create a room, make a `POST` request to `https://app.hackbox.ca/rooms`.

#### Request Headers

```json
{
  "Content-Type": "application/json"
}
```

#### Request Body

```json
{
  "hostId": "11111111-2222-3333-4444-555555555555", // A UUID that identifies your host connection. Generate this yourself. (uuid, required)
  "twitchRequired": false // Whether to require players to login with Twitch before joining your room. (boolean, optional, default: false)
}
```

#### Returns

```json
{
  "ok": true,
  "roomCode": "HKBX"
}
```

### 2. Connect to your room via websocket

Now that your room is created, anyone can connect to it via websocket, but _you_ can connect to it as the _host_ by providing the hostId you used when creating the room. Connect to `app.hackbox.ca` with your preferred websocket client, providing your `hostId` and the `roomCode` that was generated in step one.

#### Query parameters

```json
{
  "roomCode": "HKBX",
  "userId": "11111111-2222-3333-4444-555555555555"
}
```

### 3. Set up event listeners

Now it's time to set up event listeners on your host to handle the messages sent by the server.

#### state.host

`state.host` fires every time the state of the room changes. This includes when a new player joins, and when any player's online status changes.

```json
{
  "members": {
    "11111111-1111-1111-1111-111111111111": {
      "id": "11111111-1111-1111-1111-111111111111",
      "name": "Ash",
      "online": true,
      "twitchData": {
        "id": 11111,
        "username": "AT_Bash",
        "photo": ""
      }
    },
    "22222222-2222-2222-2222-222222222222": {
      "id": "22222222-2222-2222-2222-222222222222",
      "name": "Ben",
      "online": true,
      "twitchData": {
        "id": 22222,
        "username": "royal_flush",
        "photo": ""
      }
    }
  }
}
```

#### msg

`msg` is fired when a user sends you a message. This is generally done in response to a button press.

```json
{
  "event": "msg",
  "payload": {
    "from": "11111111-1111-1111-1111-111111111111",
    "event": "PickSix",
    "message": {
      "event": "PickSix",
      "value": ["B", "D", "A", "F", "E", "C"]
    },
    "timestamp": 1769260462112
  }
}
```

#### change

The `change` event is identical in shape to the `msg` event, however it represents a work in progress. The user hasn't explicitly submitted their answer; they are simply modifying a field. This can be useful if you want to accept a user's current selection when a timer expires.

```json
{
  "event": "msg",
  "payload": {
    "from": "11111111-1111-1111-1111-111111111111",
    "event": "PickSix",
    "message": {
      "event": "PickSix",
      "value": ["B", "D", "A"] // Player has only selected three of six answers.
    },
    "timestamp": 1769260462101
  }
}
```

### 4. Send your users a UI payload

With the event listeners set up, you should be receiving the `state.host` payload whenever a new user joins your room on hackbox. But, their screen will only display a welcome message for now. It's up to your host to send payloads to customize their game screens.

Here's a quick example payload that will display a single buzzer button on the player's screen.

```json
{
  "to": userIds, // Array of IDs of all users that should receive this message.
  "data": {
    {
      "ui": {
        "main": {
          "components": [
            {
              "type": "Button",
              "props": {
                "event": "MyFirstEvent",
                "label": "Buzz in",
                "value": "buzz"
              }
            }
          ]
        }
      }
    }
  }
}
```

If you've sent this event successfully, your players should see a "Buzz in" button on their screen. If your event listeners are set up correctly, when a player taps that button you will receive your first `msg` event with the following message:

```json
{
  "event": "MyFirstEvent",
  "value": "buzz"
}
```

## Next Steps

Congratulations! You're well on your way to creating your first Hackbox game! Next up, take a quick read through our game design best practices, or check out all the components you can use to create player screens!

- [Best Practices](/best-practices)
- [Components](/components)
