export default defineAppConfig({
  ui: {
    // The original admin used a #7c2fec purple accent — violet is the closest
    // Tailwind palette.
    colors: {
      primary: "violet",
      neutral: "zinc",
    },
  },
});
